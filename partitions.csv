# Name,   Type, SubType, Offset,   Size,     Flags
# Fits 4 MB flash, so the same image runs on 4, 8 and 16 MB boards. nvs and
# phy_init keep the offsets of the old layout, so a board updated from it
# keeps its settings. Two app slots for OTA later; flashing writes a blank
# otadata, which makes the bootloader start ota_0, where the app goes.
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
ota_0,    app,  ota_0,   0x10000,  0x1E0000,
ota_1,    app,  ota_1,   0x1F0000, 0x1E0000,
otadata,  data, ota,     0x3D0000, 0x2000,
