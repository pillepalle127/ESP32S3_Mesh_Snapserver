// Top-level build file. Versions are pinned to what was current/well-known
// stable at the time this was written; Android Studio will offer to bump
// AGP/Kotlin automatically if newer stable releases exist by the time this
// is opened -- accept that rather than fighting the pinned versions here.
plugins {
    id("com.android.application") version "8.13.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
